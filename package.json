{
  "name": "social-media-app",
  "version": "1.0.0",
  "description": "Connect with friends and the world around you on InThMix",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm run build-client && npm run install-server",
    "build-project": "npm run build-client && npm run install-server",
    "build-client": "cd ./client && npm install && npm run build",
    "install-server": "cd ./server && npm install",
    "start": "cd ./server && node app.js"
  },
  "author": "Demaria Woods",
  "license": "ISC",
  "cacheDirectories": [
    "client/node_modules",
    "server/node_modules"
  ]
}
